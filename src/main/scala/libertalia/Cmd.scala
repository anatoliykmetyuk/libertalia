package libertalia

/**
 * Common commands that appear in multiple entities.
 */
object Cmd {
  val exit   = "exit"
  val create = "mk"  // Make
  val update = "ch"  // Change
  val delete = "rm"  // Remove
}